apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend-container
        image: language-backend:v2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        # Backend Resources (Stress Test Profile)
        resources:
          requests:
            memory: "1.5Gi"  # High request to guarantee model allocation
            cpu: "500m"
          limits:
            memory: "4.0Gi"  # Safety margin for deserialization spikes
            cpu: "1000m"    # Capped to 1 Core
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 30 
          periodSeconds: 5
        volumeMounts:
        - name: model-volume
          mountPath: "/app/models"
          readOnly: true
      volumes:
      - name: model-volume
        hostPath:
          path: "/data/models"